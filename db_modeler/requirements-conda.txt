# Minimal requirements for conda installation
# Use this if you have SSL certificate issues

# Install these via conda-forge first:
# conda install -c conda-forge fastapi uvicorn pydantic python-dotenv jinja2 httpx aiofiles snowflake-connector-python

# Then install these via pip with --trusted-host flags:
pydantic-settings==2.1.0
openai==1.3.5
anthropic==0.7.8

# Development (optional)
# pytest==7.4.3
# pytest-asyncio==0.21.1
# black==23.11.0
# flake8==6.1.0
